import { LabelTruthToPlotValuePipe } from './label-truth-to-plot-value.pipe';

describe('LabelTruthToPlotValuePipe', () => {
  it('create an instance', () => {
    const pipe = new LabelTruthToPlotValuePipe();
    expect(pipe).toBeTruthy();
  });
});
