import { LabelTruthToPlotSourcePipe } from './label-truth-to-plot-source.pipe';

describe('LabelTruthToPlotSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new LabelTruthToPlotSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
