import { EchartsZrClickDirective } from './echarts-zr-click.directive';

describe('EchartsZrClickDirective', () => {
  it('should create an instance', () => {
    const directive = new EchartsZrClickDirective();
    expect(directive).toBeTruthy();
  });
});
