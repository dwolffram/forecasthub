import { TutorialItemDescriptionDirective } from './tutorial-item-description.directive';

describe('TutorialItemDescriptionDirective', () => {
  it('should create an instance', () => {
    const directive = new TutorialItemDescriptionDirective();
    expect(directive).toBeTruthy();
  });
});
