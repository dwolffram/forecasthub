import { LeafletResizeDirective } from './leaflet-resize.directive';

describe('LeafletResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new LeafletResizeDirective();
    expect(directive).toBeTruthy();
  });
});
